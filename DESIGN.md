# デザインルール

このサイトの見た目を変更するときの判断基準をまとめます。CSSの値を増やす前に、既存のトークンとレイアウトの責務を確認してください。

## レイアウトの責務

- `docs/.vitepress/theme/site-chrome.css` はサイト全体で共有するトークン、ヘッダー、テーマ切り替えボタンを管理する。
- `docs/.vitepress/theme/custom.css` はトップページ専用のレイアウトとコンポーネントを管理する。
- `docs/.vitepress/theme/docs-layout.css` はドキュメントページのサイドバー、本文領域、モバイルナビを管理する。
- 生成された `docs/.vitepress/dist/` は編集しない。

## 横幅

- トップページの横幅の基準は `.home-main` とする。
- `.home-main` の直下にあるヒーロー、特徴カード、案内セクション、サイト注記は、原則として `width: 100%` で同じ幅に揃える。
- 独自の最大幅を子セクションへ追加しない。狭い幅がデザイン上必要な場合は、例外であることが分かるクラス名と理由を残す。
- 共通の最大幅や左右余白は `site-chrome.css` の `--site-content-*` トークンを使う。

## デザイントークン

色、共通幅、ブレークポイントなど、複数のファイルやコンポーネントで使う値はCSSカスタムプロパティで一元管理する。

- `--site-*`: サイト全体で共有する値
- `--home-*`: トップページだけで使う値
- 新しい値を追加するときは、色そのものではなく用途を表す名前にする（例: `--site-color-brand`）。
- 1箇所でしか使わない固有値まで無理にトークン化しない。

## レスポンシブ

- サイトヘッダーとドキュメントナビのモバイル切り替えは `1100px` を境界とする。
- CSSのブレークポイントを変更する場合は、`DocsHeader.vue` の `matchMedia` も同じ境界に揃える。
- グリッドの子要素には必要に応じて `min-width: 0`、列には `minmax(0, 1fr)` を使い、内容による意図しない横幅拡張を防ぐ。
- 変更後は少なくとも 1440px、1280px、1024px、800px、390px でトップページを実ブラウザ確認する。

## ダークモード

- `html.dark` で共有トークンを上書きし、コンポーネント側で同じ色を個別に再定義しない。
- 明るいテーマ専用の背景やアクセントは、ダークモードでの可読性を確認してから追加する。

## 変更前のチェック

1. 変更対象が共通CSSか、ホーム専用CSSか、ドキュメント専用CSSかを決める。
2. 既存トークンで表現できないか確認する。
3. 親コンテナの幅や既存のブレークポイントを確認する。
4. `npm run docs:build`、`git diff --check`、Playwrightによる複数幅の表示確認を行う。
